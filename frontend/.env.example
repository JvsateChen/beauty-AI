# 采选美妆 AI · 前端环境变量样例
# 用法：cp .env.example .env.local
#
# 注意：NEXT_PUBLIC_* 变量在**构建期**被内联进浏览器产物，
# 改完必须重新 build（或重启 next dev）才会生效。
#
# 该地址是「浏览器访问后端」的地址：
#   · 本地开发      http://127.0.0.1:8000
#   · Docker 部署   http://localhost:8000（容器内网名 backend:8000 浏览器访问不到）
#   · 线上部署      你的 API 域名，如 https://api.example.com
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
